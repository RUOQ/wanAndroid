<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ruoq.wanAndroid">

    <application
        android:name="com.jess.arms.base.BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".mvp.ui.activity.main.web.WebViewActivity"
            />
        <activity android:name=".mvp.ui.activity.MainActivity" />
        <activity
            android:name=".mvp.ui.activity.start.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mvp.ui.activity.start.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize|stateHidden" />
        <activity android:name=".mvp.ui.activity.share.ShareByIdActivity" />
        <activity android:name=".mvp.ui.activity.main.tree.treeInfo.TreeInfoActivity"/>
        <activity android:name=".mvp.ui.activity.main.home.search.SearchActivity"/>
        <activity android:name=".mvp.ui.activity.main.home.search.SearchResultActivity"/>
        <activity android:name=".mvp.ui.activity.todo.TodoActivity"/>
        <activity android:name=".mvp.ui.activity.todo.AddTodoActivity"/>
        <activity android:name=".mvp.ui.activity.start.RegisterActivity" /> <!-- Arms 配置 -->
        <meta-data
            android:name="design_width_in_dp"
            android:value="375" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="667" />
        <meta-data
            android:name="com.ruoq.wanAndroid.app.GlobalConfiguration"
            android:value="ConfigModule" />
    </application>

</manifest>